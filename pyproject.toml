[project]
name = "sqldiff-ui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyinstaller>=6.22.3"
]

[dependency-groups]
dev = [
    "ruff>=0.16.1",
]
